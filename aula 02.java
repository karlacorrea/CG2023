ATIVIDADES CG

AULA 2 -
A) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
}

// draw happens every frame!

void draw () {
	rect(mouseX, mouseY, 100, 100);
}

B) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	fill(255, 0, 0);
	rect(mouseX, mouseY, 100, 100);
}

C) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	noStroke();
	fill(255, 0, 0);
	rect(mouseX, mouseY, 100, 100);
}


C) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	noStroke();
	fill(random(255), 0, 0);
	rect(mouseX, mouseY, 100, 100);
}

C) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	noStroke();
	fill(random(255), 255, random(255);
	rect(mouseX, mouseY, 100, 100);
}



D) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	noStroke();
	fill(random(255), random(255), 255);
	rect(mouseX, mouseY, 100, 100);
}

E) 02 - Colors & Variables
(Cores & Variaveis)

// setup only gets run once
void setup () {
	size(800, 600);
	
}

// draw happens every frame!

void draw () {
	noStroke();
	fill(random(255), random(255), 255);
	ellipse(mouseX, mouseY, 100, 100);
}



